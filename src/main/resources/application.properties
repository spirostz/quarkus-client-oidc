quarkus.devservices.enabled=false

quarkus.oidc.auth-server-url=${keycloak.url}/realms/quarkus/
quarkus.oidc-client.auth-server-url=${quarkus.oidc.auth-server-url}
quarkus.oidc-client.client-id=wrong_id
quarkus.oidc-client.credentials.secret=wrong_key

#quarkus.log.level=DEBUG

quarkus.rest-client.test-client.url=http://localhost:8080/
quarkus.rest-client.test-client.scope=javax.inject.Singleton